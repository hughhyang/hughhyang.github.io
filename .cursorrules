## 单主题项目规则

- 主题唯一：`个人学术网站`。`context/` 里各文件做什么、会话怎么开始/结束，**一律以 `context/README.md` 为准**（本文件不重复展开）。
- 每次会话开始时（或收到「开始同步」）：先在仓库根目录尝试 `git pull`，再读取 `context/README.md`、`context/state.md`、`context/open_questions.md`。`pull` 不可用则跳过并继续读 `context/`，在三句汇报中一句说明是否已 pull。细则见 README「开始同步时的 Git」。
- 会话记忆仅以 `context/` 下核心上下文文件为准；会话开始的 Git 约定见 README「开始同步时的 Git」；会话结束时的写入与 Git 约定见 README「超短口令」及「结束归档后的 Git」。
- 超短口令 `开始同步`、`结束归档` 的完整语义见 README「超短口令」。

## 协作偏好

- 偏好中文、简洁且结构化的回答。
- 一次会话尽量只聚焦当前单一主题。
- 若出现离题问题，先提醒是否切换主题或暂存为未决问题。
- 每次从 `context/README.md` 进入会话；会话中以 `context/state.md` 作为主状态依据。
- 不依赖原始长聊天记录，决策和问题必须显式写入 `context/` 下文件。
- 收到「结束归档」并完成 README 中规定的上下文写入后：在仓库根目录查看 `git status`；将本次归档相关改动（至少 `context/` 内；若本会话也修改了本规则文件则一并纳入）`git add`，用简短中文 `git commit`，再 `git push` 到当前分支的跟踪远程。勿将无关未提交改动混入；若无法区分则仅提交 `context/` 并告知用户。若 `push` 失败，说明原因。
