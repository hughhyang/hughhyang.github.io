## 跨设备接力聊天

本仓库用于在多台设备的 Cursor 之间无缝接续对话。

### 协作偏好
- 中文、简洁、结构化。
- 聚焦当前讨论，不做多余展开。

### 离开设备时（用户说"同步上传"或类似意图）

1. 将本次对话的**完整摘要**写入 `context/chat-summary.md`——包括：讨论了什么、做了哪些决策、代码改了什么、下一步计划是什么、有哪些未解决的问题。摘要要足够详细，让另一台设备上的 AI 能无缝接上。
2. `git add -A && git commit -m "sync: 更新聊天摘要与代码改动" && git push`。
3. 告诉用户：已推送，可以换设备继续。

### 换到新设备时（用户说"继续聊"或类似意图）

1. `git pull` 拉取最新。
2. 读取 `context/chat-summary.md`。
3. 用 2-3 句话告诉用户上次聊到哪、下一步是什么，然后等待指令。

### 注意
- `context/chat-summary.md` 每次同步时**覆盖写入**最新摘要（不是追加）。
- 如果对话中有代码修改，正常 commit 即可，同步时一并 push。
- 不需要额外的 decisions.md、sessions/ 等文件，一个摘要文件足矣。
